# Run the FastAPI application with auto-reload on port 8000
uvicorn app.main:app --reload --port 8000

docker run -d --name pg -e POSTGRES_USER=app -e POSTGRES_PASSWORD=secret -e POSTGRES_DB=appdb -p 5432:5432 postgres:15

docker exec -it pg psql -U app -d appdb -c "\dt"

docker ps
docker ps -a
docker start pg
docker stop pg
docker logs pg
docker exec -it pg psql -U app -d appdb
docker exec -it pg psql -U app -d appdb -c "\dt"
docker exec -it pg psql -U app -d appdb -c "SELECT * FROM users;"

docker images
docker rmi postgres:15
docker rm -f pg